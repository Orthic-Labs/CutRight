# CutRight — session handoff, 2026-08-02

## Answer to "is everything committed and pushed?"

**No.** `main` is pushed and clean up to `b918b07`. Two files are dirty locally
and are NOT committed:

| File | What it is | What to do |
|---|---|---|
| `apps/studio/src/styles.css` | The monochrome theme pass (graphite/tungsten/pewter). Verified working: `pnpm typecheck` clean, 67/67 tests, `pnpm build` succeeds. | Keep. Commit it once the test failure below is understood. |
| `STATUS.md` | Rewritten to match reality (see below). | Keep. Commit with the above. |
| `docs/artifacts/motion-swap-after.png` | Was **already dirty before this session started** — not mine, I never touched it. | Your call: `git checkout` it or commit it. |

## THE ONE THING TO CHECK FIRST

`bash scripts/gate.sh` currently **FAILS** at `root: cargo test --workspace
--locked`. It passed ~10 minutes earlier in the same session with the same
code. I did not get to diagnose it before the session ended — grepping the
output for `FAILED`/`panicked` returned nothing, which means the failure signal
is not where I looked.

Do this first:

```bash
cd /Volumes/D/claude/cutright && cargo test --workspace --locked 2>&1 | tail -40
```

Two plausible causes, in order:
1. **Test pollution / flake.** Several new tests shell out to real ffmpeg and
   Remotion (which downloads a Chrome Headless Shell on first run). A timeout
   under load is the most likely candidate.
2. Something in the uncommitted `styles.css` — unlikely, it is CSS only and the
   frontend suite passes.

**Do not commit until this is green or understood.** Everything else is pushed,
so there is nothing at risk in leaving the two dirty files alone.

## Where the work got to

Pushed commits this session, oldest first:

| Commit | What |
|---|---|
| `ddb7810` | Removed GitHub Actions CI — `scripts/gate.sh` is the only gate |
| `03c8a49` | REV2 P1: benchmark policy, HeardRight boundary, doctor, Studio integrity |
| `33e148b` | P1 remainder: stage receipts, per-preset QA, bounded processes |
| `747f9c9` | §14 decomposition (video-project 5528→222 lines, etc.) |
| `4742e57` | Audit-fix round 1 (variant contamination, WhisperX path, a11y, CSP) |
| `e5de253` | Audit convergence (minimize cleanup, ledger split, honest status) |
| `5ddd4fa` | §15.2 Phase 4 — captions, audio finish, colour, export |
| `c7c2296` | §15 Phases 5–7 — effect registry, semantic shorts, temporal reframing |
| `b8d1c3d` | §15 Phase 9 preference learning + real visual-support scoring |
| `5c881ab` | STATUS reflecting landed phases |
| `db4a875` | Remotion + libass renderers, cloud envelope, Settings mode, Studio redesign |
| `b918b07` | Scanner gates retired — deny.toml, unused deps removed |

The whole REV2 plan (§5–§16 hardening, §15.2–§15.7 product phases) is
implemented. Phase 8 cloud is built provider-agnostic.

## THE DECISION WAITING ON YOU

**Pick a register.** Three monochrome themes are implemented and functional but
never screenshotted (the worker producing the shots was killed mid-run):

- **graphite** — neutral near-black, bone accent `#e6e1d8`
- **tungsten** — subtle warm cast, warm bone accent `#d8c6ac`
- **pewter** — cool-neutral grey, pure white accent `#ffffff`

Light-theme accents `#1a1a1a` / `#4a3b2c`. One desaturated red-orange is
reserved for rejected/blocked/failed only.

Rationale for going monochrome at all: you rejected teal, cyan, green and blue.
A saturated accent in the chrome competes with the footage, which is the actual
content of a review tool — so state is carried by luminance, weight and
position instead. Every sibling suite accent was also already taken
(ViewRight blue, ScrapeRight gold, HeardRight ember, CodeRight crimson,
MailRight red, VoiceRight violet).

To see them:

```bash
cd apps/studio && pnpm qa:browser   # URL lands in .cache/qa-browser/url.txt
# then switch registers with the ?qa=1 register switcher
```

`brands.md` has **no CutRight entry** and must not get one until you pick.
Spec: `apps/studio/design/REDESIGN-SPEC.md`.

## Other open items (all recorded in STATUS.md)

- **Effect library is 5 of 15.** `finish.md:108` names fifteen. Renderer split
  (ffmpeg / ass / remotion / hyperframes) is done; Remotion is real and pinned
  at `4.0.503`. HyperFrames has no implementation anywhere — reserved enum
  variant that fails loudly.
- **Cloud has no provider.** Envelope complete, `Disabled` + `Fake` only.
  Consent off, budget zero, so nothing can send. Gemini / Twelve Labs are named
  in `ARCHITECTURE-2026-07-26.md:42`; the trait already models both one-shot and
  index-then-query lifecycles.
- **Your ffmpeg has no libass and no drawtext.** The ASS caption renderer fails
  loudly here; `doctor` reports it missing rather than degrading silently.
- **Rough-cut candidate generation is still gap-based**, not red-thread
  editorial. Shorts extraction IS semantic now; reframing IS temporal now.
- **Four preference axes unlearnable** until the `DecisionRecord` reason
  vocabulary is extended.

## Repo facts worth not rediscovering

- Six scanners installed this session (jscpd, knip, license-checker,
  cargo-deny, cargo-machete, cargo-geiger). hadolint is not-applicable — no
  Dockerfile. `cargo deny` and `cargo machete` are wired into the gate behind a
  presence check.
- `deny.toml` exists because unconfigured cargo-deny rejects every licence
  including MIT.
- Internal crates are `publish = false` — accurate, and it is what lets the
  path-dependency wildcard exemption apply.
- Commit `b918b07`'s message body has two phrases eaten by shell backtick
  expansion. Cosmetic; fixing it needs a force-push to `main`, which is gated.
