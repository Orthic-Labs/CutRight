# Book 6 merge receipt (CR-V2-B6-022)

Lane order applied: A → B → C → merge.

## Modes wired
home, sources, transcript, story, beats, timeline, design, motion-sound, compare, finals, qa, settings.

## Routing
Route state shape: {project_id?, revision?, timeline_id?, mode, evidence_id?, object_id?}.

## Conflicts
None observed at stub merge time. Capability guard prevents modes lacking capability from being entered; UI degrades visibly.

## Compatibility
Old review-mode route retained for migrated projects until Book 7 redirects.
