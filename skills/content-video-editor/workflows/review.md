# Review workflow

Open the review surface only after the headless artifacts exist. Every adjustment is written to
`feedback/decisions.jsonl`; UI state is never the source of truth. In reviewed mode, Adrian approves
the rough cut, finish strategy, and final export.
