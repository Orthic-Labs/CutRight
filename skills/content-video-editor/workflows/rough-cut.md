# Rough-cut workflow

Read the packed word-level transcript and decision-point filmstrips. Select one clean take per beat,
preserve real content, and separate natural and tight variants. Write `edit/candidates.json` and
`edit/cut-plan.json`; Rust validates boundaries and compiles the timeline.
