# Morph

Morph one approved visual state into next through one bounded reversible edit.
Keep source hash, frame span, easing, and crop anchor in render intent. Never mutate source pixels or resolve assets outside capability grants.
