# Heard Right (HR)

**Category**: Software — local-first dictation app with voice commands. Superwhisper-class transcription + a hands-free command layer.
**Positioning**: Local-first transcription that does more than transcribe. Wake word `zephyr` brings it to life; in-stream voice triggers (`zephyr stop`, `zephyr send`) end the utterance; 100+ standalone commands (`zephyr switch window`, `zephyr screenshot`, etc.) run actions hands-free. Any keyboard or mouse click also stops capture. **Wake word LOCKED to `zephyr` (2026-06-12)** — `quasar` dropped; no bake-off/"TBD" framing. Wake model source of truth: `heardright/docs/plans/wake_final_path_2026-07-02.md`; the `wavlm_zephyr` era is retired. Retired wake words: `quasar`, `decipher`, `cypher`, `cipher`, `shunya`.
**Tone**: Direct without curt. Honest without preachy. Confident without smug. Warm without cute. Concrete examples over abstract claims.
**Domain**: `heardright.app`
**Status**: Pre-launch foundation. The full venture source documents (`brand.md`, `business-plan.md`, `marketing-plan.md`, `copy.md`, `app-plan.md`, `roadmap.md`) live in the HeardRight venture repository; in CutRight they arrive, when installed, via the optional signed creative data pack for HR — they are NOT part of the base skill. Work from this card + data-pack contents only.

> **Positioning lock (also in root CLAUDE.md non-negotiables):** DO NOT position HR around accents, Indian English, Hindi-English, or "accents Western tools mangle." Retired. Pitch is local-first transcription + voice commands. Intelligence/LLM features are a possible later layer, NOT current positioning.

**Visual identity**: LOCKED 2026-06-05 — see **Right Suite — App Identities → HeardRight** (`skills/brand/brand-pack/right-suite.md`). Ember `#FF5630` dark / `#DF3400` light · **Tanker** (display) + **Hanken Grotesk** (body) + Spline Sans Mono · warm-coffee dark `#211D1A` / cream light `#F7F3EC`. Wordmark `HeardRight` (one word, "Right" in ember). The earlier copper `#C46A3D` + Source Serif 4 / Inter values here were a **Damned Designs copy-paste — retired**.

**Banned vocabulary**: revolutionary, disruptive, AI-powered, game-changing, empower, leverage, synergy, unlock, "limited time", "act now". (Full list in `heardright/brand.md`.)

**Content restrictions**: Use concrete command/wake-word examples with the locked wake word (e.g. "say *zephyr screenshot*", "*zephyr stop*") — the `<wake>` placeholder era is over. Do NOT write copy in `decipher`, `cypher`, `cipher`, or `shunya` — those eras are retired. No fabricated user stories. No "the only" or "the best." No accent-tuned positioning.
